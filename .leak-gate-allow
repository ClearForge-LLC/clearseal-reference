packages/core/test/vectors/*.json long-hex digests of committed public test inputs, not secrets
docs/canonical-form.md long-hex digests of committed public test inputs, not secrets
packages/core/test/fixtures/*.json long-hex digests of committed public test inputs, not secrets
pins/*.json long-hex tool and manifest digests of the committed approved manifests, not secrets
