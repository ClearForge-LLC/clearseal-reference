# dependencies and build output
node_modules/
dist/
coverage/
*.tsbuildinfo
.eslintcache

# environment and secrets — secrets are referenced by NAME only in this repo;
# a real value never lands in a tracked file (see docs/northstar.md once it exists)
.env
.env.*
!.env.example

# editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp
